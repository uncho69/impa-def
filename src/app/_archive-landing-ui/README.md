# Archivio UI landing precedente

Questa cartella contiene il backup dell’interfaccia della landing page prima del redesign (layout dark, sidebar, dashboard).

- `HomePageOriginal.tsx` – componente della vecchia home (hero, Perché ImparoDeFi, sezioni).
- Per ripristinare: copiare il contenuto in `src/app/page.tsx` e ripristinare Navbar/Footer sulla home se necessario.
